错误选项约束：
- 错误选项必须像学生真实会犯的错
- 不要使用搞笑选项、无关选项或一眼排除的选项
- 选项必须同层级，不要把“状态定义”和“输出格式”混在一题里
- 每个错误选项都必须输出一条 `distractor_feedback`
- 每条 `distractor_feedback` 必须说明这个选项具体错在哪个结构上
- 不允许只写“这个选项不对”或“这个写法错误”
- 每条 `distractor_feedback` 至少 20 字
